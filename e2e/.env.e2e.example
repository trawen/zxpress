# Copy to `.env.e2e` in this directory (gitignored). Playwright loads it automatically.
# Put the test-stand admin password in E2E_ADMIN_PASS — do not commit `.env.e2e`.

BASE_URL=https://zxpress.pst-labs.ru
PLAYWRIGHT_SKIP_WEBSERVER=1
E2E_ADMIN_USER=admin
E2E_ADMIN_PASS=
